# Powershell

function Expand-ZipFile($File, $Destination) {
  $ShellApplication = New-Object -COM Shell.Application
  $Zip = $ShellApplication.NameSpace($File)
  foreach($Item in $Zip.Items()) {
    $ShellApplication.Namespace($Destination).CopyHere($Item)
  }
}

Expand-ZipFile $args[0] $args[1]
