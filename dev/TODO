# Create ISO Repositories

# Preregister node

# Create Node
  * Master Recipe to create multiple templates
    * Support Information
    * Computer Description
    * WSUS/KMS
    Locales and Languages
  * PV Guests for Windows 10
    * PV Tools not restarting
    * XenTools, etc notloading on windows 7
    * Drop logs and optimize
    * Windows 10
  * CMD Window Buffer Height
  * Reorganize Sequences
    * Aero, etc are broken
  * Add Cloud9 Uses
  * VBS StartupScript
  * PowerShell startup
  * Get-bootstrap timeout and retry values
  * Razor callback
  * Network Locations
  * Disable Startup Tasks
  * Disable 3rd user
  * Sysprep includes from VMConfig
    * Rearm
  * Rerun sysprep before shutdown for server os
  * Refactor sequences for physical/vm nodeprep
  * Languages and Locales
  * Parameterized answerfiles/l18n.xml
    * Sysprep files for japanese fail for locales setting in winPE section
  * Checkin for node can run into continuuous loop
    * does not return an error in this state
  * Resume-ImageMaintenance 
    * Does not throw an error when considering a stage > max_stages
    * Increases the stage count incorrectly
    * Does not seem to honour stage set in runs.xml (manual case)
  * Set Administrator enabled


  REG.EXE ADD HKLM\System\CurrentControlSet\Control\Network\NewNetworkWindowOff /f
  reg add "HKLM\SOFTWARE\Microsoft\Windows NT\CurrentVersion\NetworkList\NewNetworks" /v NetworkList /t REG_MULTI_SZ /d 00000000 /f
  HLKM\SYSTEM\CurrentControlSet\Control\Network\NetworkLocationWizardHideWizard=1 
  HKCU\Software\Microsoft\Windows NT\CurrentVersion\Network\NwCategoryWizard\Show = 0
  HKCU\Software\Microsoft\Windows NT\CurrentVersion\Network\NwCategoryWizard 
  HKLM\System\CurrentControlSet\Control\Network\NewNetworkWindowOff

  Enable Administrator Account


Setup issues

sync from cloudmonkey fails on syncing apis
razor post install check for api - requiring razor-client
razor-samba does not install debian packages
razor-server package wants /usr/src/razor-server
ES dbs for iso and image are not initialized on install, instance, template, instance,box
container create on xenserver fails silently, no help either
box build fails if boot iso is not specified, should fail.
no containers found for query string - vizor container list
provide help (-h) for commands
imageprep.zip is not generated by default
task sequence failures do not keep console window open

Metadata blob
Containers
curl -s http://www.telize.com/ip 
curl 'freegeoip.net/json/185.25.64.249' | jq -S .
curl 'ipinfo.io/66.165.176.60' | jq -S .Go
curl -s 'http://www.telize.com/geoip' | jq -S .
